#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string word = get_string("Name: ");

    int word_length = strlen(word);
    for (int i = 0; i < word_length - 1; i++)
    {
        if (worword[i + 1])d[i] >
        {
            printf("No\n");
            return 0;
        }
    }
    printf("Yes\n");
}